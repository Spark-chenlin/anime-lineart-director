---
name: anime-scribble-sketch-prompt
description: 将自然语言人物需求导演成基于既定参考语料视觉 DNA 的“日系动漫手写乱线速写 / 草稿流海报”中文生图提示词，并在用户明确要求时直接生图。适用于动漫单人、情侣、多人、动作、海报、系列图和返图诊断；不用于真人摄影、普通精修线稿、纯厚涂或未受约束的跨风格混合。
metadata:
  version: "3.0.0"
---

# 日系动漫手写乱线速写导演

将简短自然语言转译成可执行的提示词或图片。目标不是泛化的“动漫手绘”，而是在参考语料归纳出的封闭视觉范围内，稳定复现清晰动漫母稿、方向性手写线层、局部密度场和克制留白。

## 默认交互

把用户的一句话视为完整委托。默认自动导演，不展示参数表，不要求用户记住模式名、L0–L4 或 D0–D4。

只有缺失信息会改变人物身份、关系或核心动作，并且无法合理推断时，才问一个最短问题。只有用户主动说“给我几个方向”“有哪些选择”“详细设置”时，才提供选项；最多给三个口语化、结果差异明显的方向。

## 任务路由

- **提示词**：用户要提示词、方案或“怎么画”时，输出可直接复制的中文完整提示词和必要的负面约束。
- **直接生图**：用户明确说“直接出图、生成图片、帮我画出来”时，先在内部完成导演，再使用可用的图像生成能力；不要用参数表反问用户。
- **系列／多版**：固定同一母稿，每版只改变一个主变量，通常输出 2–4 版。
- **方向比较／帮助**：读取 [模式与示例](references/modes-and-examples.md)，再以普通语言说明，不倾倒内部术语。
- **返图诊断／风格反推**：先检查可见证据，再指出偏离项和一个优先修正变量；需要重做时再编译修正版提示词。

处理提示词、直接生图、系列图或返图诊断时，读取：

- [视觉语法](references/visual-grammar.md)：风格边界、线层、密度、双人关系和画布合同；
- [导演工作流](references/director-workflow.md)：意图锁定、自动选择、提示词编译、闸门与修正。

自动路由到非默认主笔触、黑底反相、选择性平涂、双联画或系列变体时，也读取 [模式与示例](references/modes-and-examples.md)；普通默认头像不必加载。用户需要比较模式、解释选项或寻找示例时同样读取它。仅在维护、升级或系统性测试 Skill 时读取 [回归测试](references/eval-cases.md)。

## 用户意图优先

先保留用户明确指定的：

- 角色及身份锚点；
- 人数、关系与人物归属；
- 动作、表情、视线、接触和遮挡；
- 服装、道具、画幅、裁切和构图；
- 颜色、底色、文字及输出动作。

自动导演只补全空白，不覆盖明确要求。角色 IP 只使用稳定、可见的外观和身份锚点，不凭空增加不存在的服装、道具、关系或剧情。

## 稳定默认值

用户没有明确指定时，采用：

```text
原创动漫人物；头肩或适合动作的克制裁切；皇家蓝单色细笔；定向圆珠笔排线；不透明、中性、均匀的 #FFFFFF 纯白数字画布；无文字；无实体纸张质感；无完整场景。
```

“安静、温柔、青春、恋爱、怀旧”等情绪词只改变表情、姿态、关系和线条节奏，不能自动触发黄白纸张、彩铅、水彩、暖光、散景或精致商业插画。

## 封闭风格边界

所有自动选择必须来自本 Skill 的视觉语法。默认允许：

- 清晰动漫母稿；
- 有结构归属的手写试线、重描和排线；
- 蓝色或用户指定的受控墨线；
- 局部高密度与大面积呼吸留白；
- 克制的点色、海报布局和关系动势。

除非用户明确要求，不得加入：

- 米白、暖白、泛黄实体纸张或扫描纹理；
- 彩铅、水彩、油画、厚涂、光滑赛璐璐或普通精修线稿；
- 柔焦光晕、散景、3D 霓虹、金属体积光；
- 完整环境场景、签名、水印、角色姓名、伪日文或不可读文字。

用户明确要求范围外媒介时可以结合，但要优先保留本 Skill 的结构性手写线层，并在交付中简短说明这不是纯参考风格。

## 内部配置模型

每张图只使用：

```text
锁定意图
× 任务类型
× 1 个主笔触
× 1 个构图策略
× 1 个画布与配色策略
× 0–1 个附加效果
```

主笔触、构图、画布和效果是独立维度。黑底反相是画布策略，不是主笔触；双联画是构图策略，不是线条风格；喷溅、半调、辉光和残影线是附加效果，不得取代人物结构。

## 不可破坏的画面规则

1. 人物身份、脸型、五官、发型大形、手势、姿态、遮挡和接触边界必须先成立。
2. 眼睛、鼻口、脸部中心、手指与接触点保持低纹理保护区。
3. 高密度线必须能回指发束、衣褶、投影、动作轴或明确媒介目的。
4. 至少形成低、中、高三级密度差，禁止全画面同强度。
5. 线长、笔压、间距、起止和重描偏移自然不齐，禁止等距波浪、同半径线圈和无归属连续管线。
6. 双人或多人分别拥有个人线场；共享关系线只能服务构图和动势，不能把人物缠成一团。
7. 文字只有用户明确要求时才能出现，并且只能使用用户提供的准确内容。

## 输出

用户只要提示词时，直接交付：

```text
风格方案
[主笔触 + 构图 + 画布/配色 + 可选效果]

可直接复制的完整提示词
[连续中文自然语言段落]

负面约束
[仅保留当前画面需要的防漂移词]
```

用户要求直接生图时，以图片为主要结果，不必先展示内部参数。用户要求系列图时，先固定母稿，再逐版改变一个变量。用户要求诊断时，先说最主要的问题、证据和优先修正方式，不擅自重做图片。
