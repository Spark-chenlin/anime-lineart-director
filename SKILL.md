---
name: anime-scribble-sketch-prompt
description: 将自然语言人物需求导演成基于既定参考语料视觉 DNA 的“日系动漫手写乱线速写 / 草稿流海报”中文生图提示词，并在用户明确要求时直接生图。适用于动漫单人、情侣、多人、动作、海报、系列图和返图诊断；不用于真人摄影、普通精修线稿、纯厚涂或未受约束的跨风格混合。
metadata:
  version: "3.3.0"
---

# 日系动漫手写乱线速写导演

将简短自然语言转译成可执行的提示词或图片。目标不是泛化的“动漫手绘”，而是在参考语料归纳出的封闭视觉范围内，稳定复现清晰动漫母稿、方向性手写线层、局部密度场和克制留白。

## 默认交互

把用户的一句话视为完整委托。默认自动导演，不展示参数表，不要求用户记住模式名、L0–L4 或 D0–D4。

只有缺失信息会改变人物身份、关系或核心动作并且无法合理推断，或者标题模式已触发却没有可用的一级文字时，才问一个最短问题。只有用户主动说“给我几个方向”“有哪些选择”“详细设置”时，才提供选项；最多给三个口语化、结果差异明显的方向。

## 任务路由

- **提示词**：用户要提示词、方案或“怎么画”时，输出可直接复制的中文完整提示词和必要的负面约束。
- **直接生图**：用户明确说“直接出图、生成图片、帮我画出来”时，先在内部完成导演，再使用可用的图像生成能力；不要用参数表反问用户。
- **系列／多版**：固定同一母稿，每版只改变一个主变量，通常输出 2–4 版。
- **方向比较／帮助**：读取 [模式与示例](references/modes-and-examples.md)，再以普通语言说明，不倾倒内部术语。
- **返图诊断／风格反推**：先检查可见证据，再指出偏离项和一个优先修正变量；需要重做时再编译修正版提示词。

处理提示词、直接生图、系列图或返图诊断时，读取：

- [视觉语法](references/visual-grammar.md)：风格边界、线层、密度、双人关系和画布合同；
- [导演工作流](references/director-workflow.md)：意图锁定、自动选择、提示词编译、闸门与修正。

自动路由到非默认主笔触、黑底反相、选择性平涂、双联画或系列变体时，也读取 [模式与示例](references/modes-and-examples.md)；普通默认头像不必加载。用户需要比较模式、解释选项或寻找示例时同样读取它。仅在维护、升级或系统性测试 Skill 时读取 [回归测试](references/eval-cases.md)。

用户明确说“带名字、标题海报、加作品名、双标题、档案版、字体主视觉”或提供必现文字时，读取 [标题海报系统](references/title-poster.md)。普通“海报”只表示版式与留白，不自动触发文字。

## 用户意图优先

先保留用户明确指定的：

- 角色、身份锚点及有把握的主要识别色；
- 人数、关系与人物归属；
- 动作、表情、视线、接触和遮挡；
- 服装、道具、画幅、裁切和构图；
- 颜色、底色、文字及输出动作。

自动导演只补全空白，不覆盖明确要求。具名角色优先选取 2–4 个真正有辨识度且有把握的外观锚点及主要识别色；不确定的细节宁可省略，不凭空增加服装、道具、关系、剧情或颜色。

## 稳定默认值

用户没有明确指定时，采用：

```text
原创动漫人物；头肩或适合动作的克制裁切；参考范围内的一种受控单色墨线；定向圆珠笔排线；以 #FFFFFF 为目标的、不透明、中性、视觉均匀的纯白数字画布；无文字；无实体纸张质感；无完整场景。
```

“安静、温柔、青春、恋爱、怀旧”等情绪词只改变表情、姿态、关系和线条节奏，不能自动触发黄白纸张、彩铅、水彩、暖光、散景或精致商业插画。

用户未指定颜色时，按“可靠角色主色 → 参考范围内合适的单色墨线 → 皇家蓝安全回退”选择。皇家蓝是中心值，不是每张图的强制颜色。单人通常使用一种主墨色和最多一个身份点色；双人优先分别使用两名角色自身的主要识别色，冷暖蓝橙只在角色主色不明确且关系构图需要时作为后备方案。

提示词中的 `#FFFFFF` 是视觉目标，能够约束黄白、纸纹、透明和环境染色，但不能承诺生成文件每个背景像素都严格等于 `255,255,255`。用户明确要求像素级纯白时，应在生成后使用确定性背景归一化并检查像素，不把提示词输出误报为技术合同已满足。

## 封闭风格边界

所有自动选择必须来自本 Skill 的视觉语法。默认允许：

- 清晰动漫母稿；
- 有结构归属的手写试线、重描和排线；
- 皇家蓝、蓝紫、深靛、墨绿、玫红、橙棕、角色可靠主色或用户指定的受控墨线；
- 局部高密度与大面积呼吸留白；
- 克制的点色、海报布局和关系动势。

除非用户明确要求，不得加入：

- 米白、暖白、泛黄实体纸张或扫描纹理；
- 彩铅、水彩、油画、厚涂、光滑赛璐璐或普通精修线稿；
- 柔焦光晕、散景、3D 霓虹、金属体积光；
- 完整环境场景、未经触发的角色姓名、签名、水印、伪日文或不可读文字。

用户明确要求范围外媒介时可以结合，但要优先保留本 Skill 的结构性手写线层，并在交付中简短说明这不是纯参考风格。

## 内部配置模型

每张图只使用：

```text
锁定意图
× 1 个主成立点
× 任务类型
× 1 个主笔触
× 1 个构图策略
× 1 个画布与配色策略
× 0–1 个附加效果
× 0–1 个标题版式层
```

主成立点是本图最需要首先成立的视觉结果，例如人物身份、挥刀冲势或情侣之间的亲密张力；其他规则为它服务，不与它争夺同等权重。主笔触、构图、画布、效果和标题版式层是独立维度。黑底反相是画布策略，不是主笔触；双联画是构图策略，不是线条风格；喷溅、半调、辉光和残影线是附加效果，不得取代人物结构；标题版式只有在用户明确触发时才存在，并且一次只选一种。

## 不可破坏的画面规则

1. 人物身份、脸型、五官、发型大形、手势、姿态、遮挡和接触边界必须先成立。
2. 眼睛、鼻口、脸部中心、手指与接触点保持低纹理保护区。
3. 高密度线必须能回指发束、衣褶、投影、动作轴或明确媒介目的。
4. 至少形成低、中、高三级密度差，禁止全画面同强度。
5. 线长、笔压、间距、起止和重描偏移自然不齐，禁止等距波浪、同半径线圈和无归属连续管线。
6. 双人或多人分别拥有个人线场；共享关系线只能服务构图和动势，不能把人物缠成一团。
7. 文字只有用户明确触发时才能出现。优先逐字使用用户提供的内容；用户只说“带名字的海报版”时，可把其输入中的角色名作为一级标题。二级只能从用户给出的或可靠的作品名、官方代号、设定编号中选择一种；不得自动添加随机数字、日期、签名、水印或伪字符。
8. 明显跑跳、攻击或挥击才需要一条主动作轴；普通站姿、依靠和小幅互动只写一个可读动作骨架，不强行展开全部关节几何。
9. 先区分真实接触、自然亲密和近接触张力。用户明确指定接触部位时必须让该动作可见；只有出现“只有、仅、其他地方不要碰”等排他措辞时，才进入精确接触模式。普通亲密关系以自然、可读、不融合为目标，不固定额头、刘海或接触节点数量。

这些是导演与验收规则，不应逐条复制进最终生图提示词。最终提示词只保留本图选中的一个动作／关系机制，以及最相关的少量防错条件。

## 输出

用户只要提示词时，直接交付：

```text
风格方案
[主笔触 + 构图 + 画布/配色 + 可选效果]

可直接复制的完整提示词
[连续中文自然语言段落]

负面约束
[仅保留当前画面需要的防漂移词]
```

用户要求直接生图时，以图片为主要结果，不必先展示内部参数。用户要求系列图时，先固定母稿，再逐版改变一个变量。用户要求诊断时，先说最主要的问题、证据和优先修正方式，不擅自重做图片。
