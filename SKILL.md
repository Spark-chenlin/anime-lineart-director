---
name: anime-scribble-sketch-prompt
description: 把人物、关系动作或简单场景转成“日系动漫乱线速写 / 草稿流”风格的中文生图提示词。适用于白底单色乱线、局部彩色、渐变墨线与黑底霓虹反相变体；不用于真人摄影、厚涂、精修赛璐璐或普通干净线稿。
metadata:
  version: "1.1.0"
---

# 日系动漫乱线速写提示词

把用户的内容需求转译成可执行的乱线速写生成指令。交付的是基于视觉语法重建的同类风格提示词，不声称恢复任何原图的原始 prompt。

## 工作原则

先保留用户明确给出的角色、动作、关系、情绪、服装、画幅、颜色和文字要求。信息不足时直接采用默认值：单个原创动漫角色、头肩或半身近景、白底、皇家蓝单色墨线、无文字；不要为了补齐模板追问。

这类画面的核心不是“把线画得很多”，而是让每组笔触有结构依据、落笔节奏和停笔留白。以下层级必须同时成立：

1. 稳定、可读、略深的主轮廓锁定脸、发型、手势和身体关系。
2. 塑形乱线以分离的小组出现；每组沿同一束头发、衣褶或投影方向快速重描，组与组之间留下纸白停顿。
3. 眼睛、鼻口、手指接触点等身份区保持相对干净，不能被乱线淹没。
4. 只有少量线条越出轮廓，作为短促试探线、断开的构造弧或局部重描；不得形成连续回环、线圈或毛线团。
5. 阴影依靠线密度与交叉方向形成，不依靠平滑填色、厚涂体积或均匀网点。

任何完整提示词都必须包含这套线条系统和区域密度关系。只写“乱线、草稿、速写”或一味强调“高密度”不够。

## 手写感门槛

把乱线写成“有起笔、行进、收笔的笔触组”，而不是覆盖物体的纹理。每组线条长短不一、轻重不同、彼此大致同向；只在暗部局部交叉。头发按发束走向分组，衣物按褶皱或受力方向分组，投影按表面转折分组。相邻笔触组之间必须有可见空隙，不能从头发连续缠绕到脸、手或衣服。

生成敏感的密度顺序是：五官、面部和手指最低；发根、深色发束、衣褶背光面中高；轮廓外与背景极低。乱线均匀铺满整个主体、出现大量闭合圈和无方向长曲线时，直接判定失败，不得称为“手写乱线感”。

## 选择子模式

按用户要求选择一种；没有指定时用“白底单色”。

- 白底单色：不透明纯白纸面或白色数字画布，占画面约六至八成；全图统一使用一种蓝、黑、紫、粉或棕色墨线承担结构与明暗，禁止物体固有色回流。
- 白底局部彩色：不透明纯白底，主轮廓与乱线统一使用一种主墨色，只给虹膜、腮红、发饰或服装小块少量点色；点色不能发展成完整上色。
- 渐变墨线：同一套线条在蓝、紫、玫红、暖橙之间缓慢过渡，背景仍保持大面积白色。
- 黑底霓虹：纯黑或近黑底，青白主线叠加少量蓝紫偏移线，形成发光反相速写；保留线条层级，不能变成霓虹招牌或 3D 光效。

需要更精确的构图、线条密度、色彩比例或漂移诊断时，读取 [视觉语法](references/visual-grammar.md)。用户要求示例或输入过于抽象时，再读取 [提示词示例](references/examples.md)，只借用结构，不复制示例主体。

双人、情侣或多人近景必须使用关系优先模式：先锁定每个人的画面位置、脸部朝向、接触点、手部归属和前后遮挡，再添加线条系统。两人周围的游离线比单人图更少；不得用跨越两个人的连续长曲线制造“亲密感”。

## 组织完整提示词

按生成执行顺序写成一个连续的中文自然语言段落：

```text
媒介与子模式
→ 画幅、构图、主体占比与负空间
→ 角色身份、外观锚点与表情
→ 动作、视线、手势、接触和遮挡关系
→ 主轮廓、分组笔触、区域密度地图与局部留白
→ 墨线颜色、少量点色与背景
→ 可选手写文字的层级和位置
→ 针对当前画面的短失败防线
```

构图优先级固定为：脸部可读性与人物关系、关键手势、外轮廓、发型与服装大形、线条质感、次要装饰。复杂乱线不能排在脸、手和人物接触关系之前。

文字只有在用户明确要求时加入。描述文字的位置、大小、笔触和视觉权重；精确文案只使用用户提供的内容。默认不添加签名、水印、品牌或不可读字符。

## 漂移防线

根据当前主题选择最短的必要约束，通常包括：

```text
避免精修干净线稿、粗黑漫画描边、平滑赛璐璐色块、厚涂、写实素描、3D 渲染、均匀机械排线；避免连续回环线、闭合线圈、毛线团式曲线和全局均匀铺线；避免乱线跨越五官、接触点和手指；白底模式避免透明底、黑底反相、固有色回流和把背景填满；避免额外肢体与无意义文字。
```

不要堆砌通用质量词或互相冲突的风格标签。正向 prompt 已清楚时，负面约束只保留会改变当前画面的风险。

实际生图后按三个问题验收：主轮廓能否不依赖乱线独立读出；每组乱线是否能回指发束、衣褶或投影方向；笔触组之间是否仍有停顿留白。任一答案为否，优先减少游离线和全局密度，不要继续增加“凌乱、密集、狂草”等词。

## 默认输出

用中文交付：

```text
风格方案
[一句话说明采用的子模式、构图与主墨色]

可直接复制的完整提示词
[一个连续自然语言段落]

负面约束
[一行，仅保留当前任务需要的防漂移词]
```

用户只要求 prompt 时，省略解释，只输出“完整提示词”和“负面约束”。用户明确指定模型时，可以调整该模型的常用语法，但不改变这套视觉骨架。除非用户同时要求生图，否则不要自动生成图片。
